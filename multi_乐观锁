一. 事 务 : multi

    <1> multi 

        开启一个事务,开启后,可以写指令,但无返回值. (queued 排队)

        set age = 10

    <2> exec 

        开始执行 (提交事务)

    <3> discard (丢弃)

        事务回滚


二. 乐 观 锁 :

    乐观锁就是 对某一个key发起一个监视,然后进行进行事务,如果在事务执行之前
    
    其他用户 也监视了该key,发起了事务,并进行了改动,则其他事务 会被打断.

    哪个监视事务优先进了 CPU处理,该事务成功,其他事务被打断.
    
     
